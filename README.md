# Python03-Webscrapper
To view the headlines of news using webscrapper
A simple Python-based web scraper that extracts the latest news headlines from a news website (e.g., BBC News) using Requests and BeautifulSoup
To Install the required libraries:
pip install requests beautifulsoup4 
Save the Python code in a file named news_scraper.py Different websites use different HTML tags.
